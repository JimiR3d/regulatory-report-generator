# Generated Reports

This directory contains sample PDF outputs generated by the report engine.

## How to generate

```bash
python cli.py generate --input data/sample_cbn_return.csv --output reports/sample_output.pdf
```

> **Note:** A pre-generated sample PDF will be added once WeasyPrint dependencies are configured in the CI environment. Run the command above locally to generate your own copy.
